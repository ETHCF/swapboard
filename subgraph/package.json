{
  "name": "otcboard-subgraph",
  "version": "1.0.0",
  "private": true,
  "description": "The Graph subgraph for OTCBoard contract indexing",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    "deploy": "graph deploy --studio otcboard",
    "create-local": "graph create --node http://localhost:8020/ otcboard",
    "remove-local": "graph remove --node http://localhost:8020/ otcboard",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 otcboard"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.71.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
